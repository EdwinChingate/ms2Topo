## Description

he variable `parameter` is a single numerical value representing a peak parameter (such as RT, intensity, or standard deviation) in the `MutateParameter` function, which is mutated within its defined boundaries to create new parameter values.

---

## Functions using

- [[MutateParameter]]
