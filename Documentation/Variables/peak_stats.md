## Description

peak_stats` is a list variable that contains the statistical properties of a single peak, which are calculated by the `ms2_peak_stats` function and then used for feature characterization, data filtering and data organization. The `ms2_peakStats_safe` function handles errors related to its calculation.

---

## Functions using

- [[ms2_peak_stats]]
- [[ms2_peakStats_safe]]
