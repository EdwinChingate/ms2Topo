## Description

Neigbours` is a numpy array of integers representing the indices of neighboring spectra, which is used by `CheckNeigbours` to determine if those neighboring spectra are similar to a given spectra, based on user-defined tolerances for RT, m/z, and spectral similarity.

---

## Functions using

- [[CheckNeigbours]]
