## Description

count` is an integer that acts as a recursion counter in `ms2Peak`, `Find_ms2Peak`, `mzPeak`, and `Find_mzPeak`, limiting the number of recursive calls and increasing the `stdDistance` parameter in each recursive step.

---

## Functions using

- [[ms2Peak]]
- [[Find_ms2Peak]]
- [[mzPeak]]
- [[Find_mzPeak]]
