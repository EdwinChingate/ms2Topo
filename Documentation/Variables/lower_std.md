## Description

lower_std` isn't a variable but a selection criterion used by `BiggestSelector`. When multiple individuals have the same r2 value, the individual with the lowest standard deviation (`lower_std`) is chosen.

---

## Functions using

- [[BiggestSelector]]
