## Description

Parameters` is a mixed-type list containing the parameters of a peak, used by `Parameters_to_string` to transform it into a string and by `WriteLog` to save it to a log file, helping keep track of information and potential issues during the analysis.

---

## Functions using

- [[Parameters_to_string]]
- [[WriteLog]]
