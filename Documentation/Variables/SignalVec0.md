## Description

SignalVec0` is a NumPy array of signal intensity values, used by `DistanceDistribution` to compute signal distance distributions, and by `LowSignalClustering` as the input for clustering low intensity signals.

---

## Functions using

- [[DistanceDistribution]]
- [[LowSignalClustering]]
