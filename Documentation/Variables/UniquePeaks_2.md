## Description

UniquePeaks_2` is a NumPy array that stores the indices of unique peaks in `Spectrum_2`. It's used in `OverlpaingPeaks` to find peaks that do not overlap with those of `Spectrum_1`.

---

## Functions using

