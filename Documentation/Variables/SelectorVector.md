## Description

SelectorVector` is a NumPy array of binary values that acts as a mask, used in `OneZero` to generate lists of binary vectors for peak selection.

---

## Functions using

- [[OneZero]]
