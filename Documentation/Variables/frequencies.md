## Description

frequencies` is a NumPy array, generated in `SmoothFourier`, that holds the frequency values corresponding to an FFT signal. In `CuttingFreq` it's used to calculate a frequency threshold for signal processing.

---

## Functions using

- [[CuttingFreq]]
