## Description

UniqueParametersMat` is likely a NumPy array, used in `UniquePeaks` to store the parameters of unique peaks, although it is not defined in the provided code.

---

## Functions using

- [[UniquePeaks]]
