## Description

RelativeContribution` is a boolean in `Find_ms2Peak`, that when true, calculates and returns the intensity of a peak relative to the total spectrum intensity, otherwise returns only the peak data.

---

## Functions using

- [[Find_ms2Peak]]
