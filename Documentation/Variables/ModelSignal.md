## Description

ModelSignal` is a numpy array that contains the intensities of a signal as predicted by a model, and it's used by the `r2_Model` function to evaluate the model's fit to the raw data, through the R-squared value calculation.

---

## Functions using

- [[r2_Model]]
